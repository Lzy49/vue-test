<template>
  <span>
    1. 使用 globalProperties 时需要补充声明文件。 typescript 声明文件存放在
    根目录的 typings 里 <br />
    2. 使用 computed 时 应有 返回值类型。<br />
    3. 使用 interface 时 在 date 返回值中直接使用 as 关键字，在 proper
    中需要使用PropType&lttype>的形式
  </span>
</template>
